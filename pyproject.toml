[tool.poetry]
name = "file-transfer"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"


[tool.poetry.dependencies]
python = "^3.10"
pydicom = "^3.0.0"
pandas = "^2.2.2"
click = "^8.1.7"

[tool.poetry.scripts]
file-transfer = "file_transfer.cli:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
